{
  "name": "@vinyl-webshop/server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --target \"node\" --outdir ./dist index.ts",
    "start": "fastify start -l info -w /dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@vinyl-webshop/database": "workspace:*",
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.6.0",
    "fastify-cli": "^7.4.0",
    "zod": "^4.1.9"
  }
}
